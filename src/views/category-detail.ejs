<%- include('partials/header') %>

<h2>Categoría: <%= category.name %></h2>

<div class="project-card-detail" style="background:#f8f9fa; padding:20px; border-radius:5px; margin-top:1.5rem;">
  <p><strong>ID:</strong> <%= category.category_id %></p>

  <h3 style="margin-top:1.5rem;">Proyectos en esta categoría</h3>
  <% if (projects && projects.length > 0) { %>
    <ul style="line-height: 2;">
      <% projects.forEach(p => { %>
        <li>
          <a href="/project/<%= p.project_id %>"
             style="color:#28a745; font-weight:bold; text-decoration:none;">
            <%= p.name %>
          </a>
          <small style="color:#6c757d;">
            (<%= p.date ? new Date(p.date).toLocaleDateString() : 'Sin fecha' %>)
          </small>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p style="color:#6c757d; font-style:italic;">
      Todavía no hay proyectos asociados a esta categoría.
    </p>
  <% } %>

  <div style="margin-top:1.5rem;">
    <a href="/edit-category/<%= category.category_id %>"
       style="color:#007bff;">✏️ Editar categoría</a>
  </div>
</div>

<div style="margin-top:2rem;">
  <a href="/categories" style="color:#007bff; text-decoration:none;">← Volver a Categorías</a>
</div>

<%- include('partials/footer') %>

